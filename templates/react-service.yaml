apiVersion: v1
kind: Service
metadata:
  name: react-service

spec:
  type: LoadBalancer

  selector:
    app: react

  ports:
    - port: {{ .Values.react.port }}
      targetPort: {{ .Values.react.port }}
